# @lsq/automation-ids

## 0.2.0

### Minor Changes

- 56e4d10: Add new `@lsq/automation-ids` package with `generateAutomationId`, `AutomationIds`, `withAutomationId`, and `useAutomationId` for generating consistent `data-automation-id` values across applications
